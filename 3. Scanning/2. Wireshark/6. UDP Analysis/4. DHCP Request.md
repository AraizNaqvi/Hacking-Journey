
The Client still refers to its IP address as 0.0.0.0 but sends a broadcast to that specific DHCP server to allot it the IP Address it was offered in the DHCP Offer.

The other options like subnet, router, DNS, etc are still empty and will be requested for in the ACK phase.